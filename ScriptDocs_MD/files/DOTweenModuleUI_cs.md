# File: DOTweenModuleUI.cs
[< トップページへ戻る](../Index.md)

> **Path:** `Assets/Plugins/Demigiant/DOTween/Modules/DOTweenModuleUI.cs`

<a id='DOTweenModuleUI'></a>
## Class DOTweenModuleUI
**Namespace:** `DG.Tweening`<br>
**Signature:** `DOTweenModuleUI`

### メソッド (Methods)
| アクセス | 名前 | 引数 | 戻り値 | 説明 |
| :---: | :--- | :--- | :---: | :--- |
| `public` | S DOFade | CanvasGroup target, Single endValue, Single duration | `TweenerCore`3` | Tweens a CanvasGroup's alpha color to the given value.<br>Also stores the canvasGroup as the tween's target so it can be used for filtered operations<br>Tweens an Graphic's alpha color to the given value.<br>Also stores the image as the tween's target so it can be used for filtered operations<br>Tweens an Image's alpha color to the given value.<br>Also stores the image as the tween's target so it can be used for filtered operations<br>Tweens a Outline's effectColor alpha to the given value.<br>Also stores the Outline as the tween's target so it can be used for filtered operations<br>Tweens a Text's alpha color to the given value.<br>Also stores the Text as the tween's target so it can be used for filtered operations |
| `public` | S DOColor | Graphic target, Color endValue, Single duration | `TweenerCore`3` | Tweens an Graphic's color to the given value.<br>Also stores the image as the tween's target so it can be used for filtered operations<br>Tweens an Image's color to the given value.<br>Also stores the image as the tween's target so it can be used for filtered operations<br>Tweens a Outline's effectColor to the given value.<br>Also stores the Outline as the tween's target so it can be used for filtered operations<br>Tweens a Text's color to the given value.<br>Also stores the Text as the tween's target so it can be used for filtered operations |
| `public` | S DOFade | Graphic target, Single endValue, Single duration | `TweenerCore`3` | Tweens a CanvasGroup's alpha color to the given value.<br>Also stores the canvasGroup as the tween's target so it can be used for filtered operations<br>Tweens an Graphic's alpha color to the given value.<br>Also stores the image as the tween's target so it can be used for filtered operations<br>Tweens an Image's alpha color to the given value.<br>Also stores the image as the tween's target so it can be used for filtered operations<br>Tweens a Outline's effectColor alpha to the given value.<br>Also stores the Outline as the tween's target so it can be used for filtered operations<br>Tweens a Text's alpha color to the given value.<br>Also stores the Text as the tween's target so it can be used for filtered operations |
| `public` | S DOColor | Image target, Color endValue, Single duration | `TweenerCore`3` | Tweens an Graphic's color to the given value.<br>Also stores the image as the tween's target so it can be used for filtered operations<br>Tweens an Image's color to the given value.<br>Also stores the image as the tween's target so it can be used for filtered operations<br>Tweens a Outline's effectColor to the given value.<br>Also stores the Outline as the tween's target so it can be used for filtered operations<br>Tweens a Text's color to the given value.<br>Also stores the Text as the tween's target so it can be used for filtered operations |
| `public` | S DOFade | Image target, Single endValue, Single duration | `TweenerCore`3` | Tweens a CanvasGroup's alpha color to the given value.<br>Also stores the canvasGroup as the tween's target so it can be used for filtered operations<br>Tweens an Graphic's alpha color to the given value.<br>Also stores the image as the tween's target so it can be used for filtered operations<br>Tweens an Image's alpha color to the given value.<br>Also stores the image as the tween's target so it can be used for filtered operations<br>Tweens a Outline's effectColor alpha to the given value.<br>Also stores the Outline as the tween's target so it can be used for filtered operations<br>Tweens a Text's alpha color to the given value.<br>Also stores the Text as the tween's target so it can be used for filtered operations |
| `public` | S DOFillAmount | Image target, Single endValue, Single duration | `TweenerCore`3` | Tweens an Image's fillAmount to the given value.<br>Also stores the image as the tween's target so it can be used for filtered operations |
| `public` | S DOGradientColor | Image target, Gradient gradient, Single duration | `Sequence` | Tweens an Image's colors using the given gradient<br>(NOTE 1: only uses the colors of the gradient, not the alphas - NOTE 2: creates a Sequence, not a Tweener).<br>Also stores the image as the tween's target so it can be used for filtered operations |
| `public` | S DOFlexibleSize | LayoutElement target, Vector2 endValue, Single duration, Boolean snapping | `TweenerCore`3` | Tweens an LayoutElement's flexibleWidth/Height to the given value.<br>Also stores the LayoutElement as the tween's target so it can be used for filtered operations |
| `public` | S DOMinSize | LayoutElement target, Vector2 endValue, Single duration, Boolean snapping | `TweenerCore`3` | Tweens an LayoutElement's minWidth/Height to the given value.<br>Also stores the LayoutElement as the tween's target so it can be used for filtered operations |
| `public` | S DOPreferredSize | LayoutElement target, Vector2 endValue, Single duration, Boolean snapping | `TweenerCore`3` | Tweens an LayoutElement's preferredWidth/Height to the given value.<br>Also stores the LayoutElement as the tween's target so it can be used for filtered operations |
| `public` | S DOColor | Outline target, Color endValue, Single duration | `TweenerCore`3` | Tweens an Graphic's color to the given value.<br>Also stores the image as the tween's target so it can be used for filtered operations<br>Tweens an Image's color to the given value.<br>Also stores the image as the tween's target so it can be used for filtered operations<br>Tweens a Outline's effectColor to the given value.<br>Also stores the Outline as the tween's target so it can be used for filtered operations<br>Tweens a Text's color to the given value.<br>Also stores the Text as the tween's target so it can be used for filtered operations |
| `public` | S DOFade | Outline target, Single endValue, Single duration | `TweenerCore`3` | Tweens a CanvasGroup's alpha color to the given value.<br>Also stores the canvasGroup as the tween's target so it can be used for filtered operations<br>Tweens an Graphic's alpha color to the given value.<br>Also stores the image as the tween's target so it can be used for filtered operations<br>Tweens an Image's alpha color to the given value.<br>Also stores the image as the tween's target so it can be used for filtered operations<br>Tweens a Outline's effectColor alpha to the given value.<br>Also stores the Outline as the tween's target so it can be used for filtered operations<br>Tweens a Text's alpha color to the given value.<br>Also stores the Text as the tween's target so it can be used for filtered operations |
| `public` | S DOScale | Outline target, Vector2 endValue, Single duration | `TweenerCore`3` | Tweens a Outline's effectDistance to the given value.<br>Also stores the Outline as the tween's target so it can be used for filtered operations |
| `public` | S DOAnchorPos | RectTransform target, Vector2 endValue, Single duration, Boolean snapping | `TweenerCore`3` | Tweens a RectTransform's anchoredPosition to the given value.<br>Also stores the RectTransform as the tween's target so it can be used for filtered operations |
| `public` | S DOAnchorPosX | RectTransform target, Single endValue, Single duration, Boolean snapping | `TweenerCore`3` | Tweens a RectTransform's anchoredPosition X to the given value.<br>Also stores the RectTransform as the tween's target so it can be used for filtered operations |
| `public` | S DOAnchorPosY | RectTransform target, Single endValue, Single duration, Boolean snapping | `TweenerCore`3` | Tweens a RectTransform's anchoredPosition Y to the given value.<br>Also stores the RectTransform as the tween's target so it can be used for filtered operations |
| `public` | S DOAnchorPos3D | RectTransform target, Vector3 endValue, Single duration, Boolean snapping | `TweenerCore`3` | Tweens a RectTransform's anchoredPosition3D to the given value.<br>Also stores the RectTransform as the tween's target so it can be used for filtered operations |
| `public` | S DOAnchorPos3DX | RectTransform target, Single endValue, Single duration, Boolean snapping | `TweenerCore`3` | Tweens a RectTransform's anchoredPosition3D X to the given value.<br>Also stores the RectTransform as the tween's target so it can be used for filtered operations |
| `public` | S DOAnchorPos3DY | RectTransform target, Single endValue, Single duration, Boolean snapping | `TweenerCore`3` | Tweens a RectTransform's anchoredPosition3D Y to the given value.<br>Also stores the RectTransform as the tween's target so it can be used for filtered operations |
| `public` | S DOAnchorPos3DZ | RectTransform target, Single endValue, Single duration, Boolean snapping | `TweenerCore`3` | Tweens a RectTransform's anchoredPosition3D Z to the given value.<br>Also stores the RectTransform as the tween's target so it can be used for filtered operations |
| `public` | S DOAnchorMax | RectTransform target, Vector2 endValue, Single duration, Boolean snapping | `TweenerCore`3` | Tweens a RectTransform's anchorMax to the given value.<br>Also stores the RectTransform as the tween's target so it can be used for filtered operations |
| `public` | S DOAnchorMin | RectTransform target, Vector2 endValue, Single duration, Boolean snapping | `TweenerCore`3` | Tweens a RectTransform's anchorMin to the given value.<br>Also stores the RectTransform as the tween's target so it can be used for filtered operations |
| `public` | S DOPivot | RectTransform target, Vector2 endValue, Single duration | `TweenerCore`3` | Tweens a RectTransform's pivot to the given value.<br>Also stores the RectTransform as the tween's target so it can be used for filtered operations |
| `public` | S DOPivotX | RectTransform target, Single endValue, Single duration | `TweenerCore`3` | Tweens a RectTransform's pivot X to the given value.<br>Also stores the RectTransform as the tween's target so it can be used for filtered operations |
| `public` | S DOPivotY | RectTransform target, Single endValue, Single duration | `TweenerCore`3` | Tweens a RectTransform's pivot Y to the given value.<br>Also stores the RectTransform as the tween's target so it can be used for filtered operations |
| `public` | S DOSizeDelta | RectTransform target, Vector2 endValue, Single duration, Boolean snapping | `TweenerCore`3` | Tweens a RectTransform's sizeDelta to the given value.<br>Also stores the RectTransform as the tween's target so it can be used for filtered operations |
| `public` | S DOPunchAnchorPos | RectTransform target, Vector2 punch, Single duration, Int32 vibrato, Single elasticity, Boolean snapping | `Tweener` | Punches a RectTransform's anchoredPosition towards the given direction and then back to the starting one<br>as if it was connected to the starting position via an elastic.<br>Also stores the RectTransform as the tween's target so it can be used for filtered operations |
| `public` | S DOShakeAnchorPos | RectTransform target, Single duration, Single strength, Int32 vibrato, Single randomness, Boolean snapping, Boolean fadeOut, ShakeRandomnessMode randomnessMode | `Tweener` | Shakes a RectTransform's anchoredPosition with the given values.<br>Also stores the RectTransform as the tween's target so it can be used for filtered operations |
| `public` | S DOShakeAnchorPos | RectTransform target, Single duration, Vector2 strength, Int32 vibrato, Single randomness, Boolean snapping, Boolean fadeOut, ShakeRandomnessMode randomnessMode | `Tweener` | Shakes a RectTransform's anchoredPosition with the given values.<br>Also stores the RectTransform as the tween's target so it can be used for filtered operations |
| `public` | S DOJumpAnchorPos | RectTransform target, Vector2 endValue, Single jumpPower, Int32 numJumps, Single duration, Boolean snapping | `Sequence` | Tweens a RectTransform's anchoredPosition to the given value, while also applying a jump effect along the Y axis.<br>Returns a Sequence instead of a Tweener.<br>Also stores the RectTransform as the tween's target so it can be used for filtered operations |
| `public` | S DONormalizedPos | ScrollRect target, Vector2 endValue, Single duration, Boolean snapping | `Tweener` | Tweens a ScrollRect's horizontal/verticalNormalizedPosition to the given value.<br>Also stores the ScrollRect as the tween's target so it can be used for filtered operations |
| `public` | S DOHorizontalNormalizedPos | ScrollRect target, Single endValue, Single duration, Boolean snapping | `Tweener` | Tweens a ScrollRect's horizontalNormalizedPosition to the given value.<br>Also stores the ScrollRect as the tween's target so it can be used for filtered operations |
| `public` | S DOVerticalNormalizedPos | ScrollRect target, Single endValue, Single duration, Boolean snapping | `Tweener` | Tweens a ScrollRect's verticalNormalizedPosition to the given value.<br>Also stores the ScrollRect as the tween's target so it can be used for filtered operations |
| `public` | S DOValue | Slider target, Single endValue, Single duration, Boolean snapping | `TweenerCore`3` | Tweens a Slider's value to the given value.<br>Also stores the Slider as the tween's target so it can be used for filtered operations |
| `public` | S DOColor | Text target, Color endValue, Single duration | `TweenerCore`3` | Tweens an Graphic's color to the given value.<br>Also stores the image as the tween's target so it can be used for filtered operations<br>Tweens an Image's color to the given value.<br>Also stores the image as the tween's target so it can be used for filtered operations<br>Tweens a Outline's effectColor to the given value.<br>Also stores the Outline as the tween's target so it can be used for filtered operations<br>Tweens a Text's color to the given value.<br>Also stores the Text as the tween's target so it can be used for filtered operations |
| `public` | S DOCounter | Text target, Int32 fromValue, Int32 endValue, Single duration, Boolean addThousandsSeparator, CultureInfo culture | `TweenerCore`3` | Tweens a Text's text from one integer to another, with options for thousands separators |
| `public` | S DOFade | Text target, Single endValue, Single duration | `TweenerCore`3` | Tweens a CanvasGroup's alpha color to the given value.<br>Also stores the canvasGroup as the tween's target so it can be used for filtered operations<br>Tweens an Graphic's alpha color to the given value.<br>Also stores the image as the tween's target so it can be used for filtered operations<br>Tweens an Image's alpha color to the given value.<br>Also stores the image as the tween's target so it can be used for filtered operations<br>Tweens a Outline's effectColor alpha to the given value.<br>Also stores the Outline as the tween's target so it can be used for filtered operations<br>Tweens a Text's alpha color to the given value.<br>Also stores the Text as the tween's target so it can be used for filtered operations |
| `public` | S DOText | Text target, String endValue, Single duration, Boolean richTextEnabled, ScrambleMode scrambleMode, String scrambleChars | `TweenerCore`3` | Tweens a Text's text to the given value.<br>Also stores the Text as the tween's target so it can be used for filtered operations |
| `public` | S DOBlendableColor | Graphic target, Color endValue, Single duration | `Tweener` | Tweens a Graphic's color to the given value,<br>in a way that allows other DOBlendableColor tweens to work together on the same target,<br>instead than fight each other as multiple DOColor would do.<br>Also stores the Graphic as the tween's target so it can be used for filtered operations<br>Tweens a Image's color to the given value,<br>in a way that allows other DOBlendableColor tweens to work together on the same target,<br>instead than fight each other as multiple DOColor would do.<br>Also stores the Image as the tween's target so it can be used for filtered operations<br>Tweens a Text's color BY the given value,<br>in a way that allows other DOBlendableColor tweens to work together on the same target,<br>instead than fight each other as multiple DOColor would do.<br>Also stores the Text as the tween's target so it can be used for filtered operations |
| `public` | S DOBlendableColor | Image target, Color endValue, Single duration | `Tweener` | Tweens a Graphic's color to the given value,<br>in a way that allows other DOBlendableColor tweens to work together on the same target,<br>instead than fight each other as multiple DOColor would do.<br>Also stores the Graphic as the tween's target so it can be used for filtered operations<br>Tweens a Image's color to the given value,<br>in a way that allows other DOBlendableColor tweens to work together on the same target,<br>instead than fight each other as multiple DOColor would do.<br>Also stores the Image as the tween's target so it can be used for filtered operations<br>Tweens a Text's color BY the given value,<br>in a way that allows other DOBlendableColor tweens to work together on the same target,<br>instead than fight each other as multiple DOColor would do.<br>Also stores the Text as the tween's target so it can be used for filtered operations |
| `public` | S DOBlendableColor | Text target, Color endValue, Single duration | `Tweener` | Tweens a Graphic's color to the given value,<br>in a way that allows other DOBlendableColor tweens to work together on the same target,<br>instead than fight each other as multiple DOColor would do.<br>Also stores the Graphic as the tween's target so it can be used for filtered operations<br>Tweens a Image's color to the given value,<br>in a way that allows other DOBlendableColor tweens to work together on the same target,<br>instead than fight each other as multiple DOColor would do.<br>Also stores the Image as the tween's target so it can be used for filtered operations<br>Tweens a Text's color BY the given value,<br>in a way that allows other DOBlendableColor tweens to work together on the same target,<br>instead than fight each other as multiple DOColor would do.<br>Also stores the Text as the tween's target so it can be used for filtered operations |
| `public` | S DOShapeCircle | RectTransform target, Vector2 center, Single endValueDegrees, Single duration, Boolean relativeCenter, Boolean snapping | `TweenerCore`3` | Tweens a RectTransform's anchoredPosition so that it draws a circle around the given center.<br>Also stores the RectTransform as the tween's target so it can be used for filtered operations.<br>IMPORTANT: SetFrom(value) requires a  instead of a float, where the X property represents the "from degrees value" |

---

